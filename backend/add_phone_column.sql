-- ============================================
-- 물음송이 DB 업데이트: 회원가입에 전화번호(선택) 컬럼 추가
-- 목적: 회원가입 폼에 전화번호 입력 칸을 추가하면서, email과 동일하게
--       선택 입력 + NULL 허용 컬럼으로 users 테이블에 추가
-- ============================================

ALTER TABLE users ADD COLUMN IF NOT EXISTS phone VARCHAR(20);

SELECT '전화번호 컬럼 추가 완료!' AS message;
